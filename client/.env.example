# Backend URL (used by Next.js rewrites at build time)
API_URL=http://localhost:4000

# Optional: direct API URL for browser requests in production
# NEXT_PUBLIC_API_URL=https://your-api.onrender.com
